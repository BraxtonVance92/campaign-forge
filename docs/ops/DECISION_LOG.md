# CampaignForge Decision Log

Only founder-approved decisions are binding. `PROPOSED` entries are recommendations awaiting approval. Every entry records status, date, authority, rationale, and supersession links where applicable. Never overwrite history silently — corrections are recorded as new rows.

| ID | Date | Status | Decision | Reason / consequence | Authority | Supersedes / Superseded by |
|---|---|---|---|---|---|---|
| D-001 | 2026-07-21 | APPROVED-HISTORICAL | CampaignForge focuses on an AI content studio that learns from authorized creator videos/carousels. | Preserves the founder's chosen product direction. | Founder | — |
| D-002 | 2026-07-21 | APPROVED-HISTORICAL | Spending ceiling is $50 unless the founder changes it. | Cost estimates and actual recorded charges remain separate. | Founder | — |
| D-003 | 2026-07-21 | APPROVED-HISTORICAL | GPT is controller/CTO; Claude Code is maker; founder is final product authority. | Separates implementation from review and prevents unsupported completion claims. | Founder | — |
| D-004 | 2026-07-21 | SUPERSEDED by D-010 | Contest MVP golden path is authorized example -> initial fingerprint -> new idea -> branded generated asset -> B2 persistence/restore with Genblaze provenance. | Covered the creator problem and contest criteria without face/voice generation. | Founder (original; since corrected) | Superseded by D-010 |
| D-005 | 2026-07-21 | REJECTED, SUPERSEDED by D-011 | Face and voice cloning are excluded from the contest MVP. | Consent, quality, schedule, and deception risks were judged to outweigh contest value; the founder subsequently rejected this judgment as incorrect. | Founder (original; since rejected by the same founder) | Superseded by D-011 |
| D-006 | 2026-07-21 | PROPOSED | Use Python/FastAPI for the Genblaze service and a React/Next.js web UI, based on current official samples. | Minimizes integration risk with the Python SDK while providing a polished web journey. Technical stack choice, delegated to controller; not yet founder-approved as binding. | Controller/CTO (recommendation) | — |
| D-007 | 2026-07-21 | PROPOSED | Use GMI Cloud through the Genblaze adapter for image/video generation; select exact models only after live capability/cost tests. | GMI is contest-supported and one key is reportedly available, but current access, model behavior, and whether it supports voice cloning or face/avatar generation are not verified. See Phase 0.5 in `docs/roadmap/CURRENT_ROADMAP.md`. | Controller/CTO (recommendation) | — |
| D-008 | 2026-07-21 | PROPOSED | Feature freeze July 31; target submission August 2, one day before deadline. | Preserves final-day buffer. Schedule guidance only; does not authorize cutting face or voice generation to hit the date. | Controller/CTO (recommendation) | — |
| D-009 | 2026-07-21 | PROPOSED | Pricing, billing, publishing, teams, analytics scraping, and multi-model fan-out are post-contest. | Keeps the submission complete and reliable. This list explicitly excludes face and voice generation, which are core and not deferred (see D-011). | Controller/CTO (recommendation) | Clarified by D-011 |
| D-010 | 2026-07-21 | APPROVED | CampaignForge's target end-to-end flow is: one authorized creator -> authorized reference inputs and consent -> real creator analysis -> editable creator profile -> idea and script -> cloned voice -> face/avatar generation -> playable final video -> B2 persistence and restoration with provenance. | Founder correction: authorized creator replication (face and voice) is the core product, not a generic branded-asset generator. The initial implementation narrows to one authorized creator to fit schedule, without removing face or voice generation. | Founder | Supersedes D-004 |
| D-011 | 2026-07-21 | APPROVED | The earlier proposal excluding face and voice cloning (D-005) is REJECTED and SUPERSEDED. Authorized face-and-voice generation is the core product. Verified, person-specific creator consent is mandatory before analysis, cloning, or generation involving that person. The narrow first version supports one authorized creator rather than removing the capability. | Delivered directly by the founder as a binding correction. Consent remains a non-negotiable safety control, distinct from the founder's own project-level authorization. | Founder | Supersedes D-005 |
| D-012 | 2026-07-21 | APPROVED | PR #1 (the CF-BOOT-001 docs-only bootstrap pack, including the D-010/D-011 correction) is approved as-is at exact SHA `9ae223920b0f71aed1831070a5ba9f4924683aec` and merged into `main` at merge commit `3e06fc2e076f09c7b077d3f5e803583cd0ada5e4`. | Controller/CTO review confirmed the corrected canon is coherent and cleared the packet for merge. | Controller/CTO | — |
| D-013 | 2026-07-21 | APPROVED | `CF-VERIFY-001` (live provider capability verification) is dispatched as the next active work packet: authorize the smallest GMI/Genblaze live tests first (voice cloning and whether the GMI avatar endpoint can produce a downloadable video), maximum initial spend $5, recorded call-by-call. GMI is the first provider to test; HeyGen is considered only if live GMI testing proves GMI cannot produce the required reusable face/video result. | Controller/CTO directed this as the next action after merge, narrower than full runtime implementation. | Controller/CTO | — |
| D-014 | 2026-07-21 | APPROVED, refined by D-015/D-016 | `CF-VERIFY-001`'s forbidden-paths list is amended to permit exactly one CI file, `.github/workflows/cf-verify-001.yml`, as the secret-entry and execution mechanism for the GMI live tests. The workflow is manually triggered only (`workflow_dispatch`, never on push/PR/schedule) and reads `GMI_API_KEY` from GitHub repository secrets. | No secret-entry UI exists in the maker's session and terminal/manual-file approaches were declined; GitHub Actions repository secrets is the remaining click-only, secure secret-entry path. | Controller/CTO | Narrows the general CI-file prohibition in `CF-VERIFY-001` for this one file only |
| D-015 | 2026-07-21 | APPROVED | Security revision to the CF-VERIFY-001 workflow: destination endpoints must be hardcoded (verified `https://console.gmicloud.ai/api/v1/ie/requestqueue/apikey/requests`, used by both models) plus a runtime exact-hostname allowlist check before the secret is ever attached to a request; the informal `$5 cap` is replaced with two fixed, hardcoded, non-repeatable calls whose documented worst-case combined cost is $1.20; Test 2 performs one real, minimal `heygen-avatar-4` request (not a metadata-only listing) to actually determine downloadable-file vs. WebRTC-only output; raw API response bodies are never logged, only an explicit sanitized-field allowlist; the reference audio clip for Test 1 is generated by the workflow itself (on-runner synthetic speech, non-personal) rather than requiring the founder to locate or host one. | Prior draft let a dispatch input choose the destination host a bearer token would be sent to (a secret-exfiltration risk), treated the spend cap as an unenforced reminder, and answered the avatar question only by listing models rather than testing generation. | Controller/CTO | Refines D-014 |
| D-016 | 2026-07-21 | APPROVED | Merging PR #2 (CF-VERIFY-001's documentation and the one reviewed workflow file) into `main` is authorized as the step required to make `workflow_dispatch` reliably triggerable, since GitHub's manual-dispatch mechanism expects the workflow to be registered from the default branch. This is distinct from, and does not itself authorize, triggering the workflow — that remains a separate action requiring a human to add the `GMI_API_KEY` secret and click "Run workflow." | `workflow_dispatch` on a feature-branch-only workflow is not reliably dispatchable via the GitHub UI or API. | Controller/CTO | Refines D-014 |
| D-017 | 2026-07-21 | APPROVED | The $1.20 in-code worst-case ceiling (D-015) is superseded: the real spending boundary for CF-VERIFY-001's two live calls is a **$20 maximum prepaid GMI account credit balance**, not an estimated dollar figure computed from GMI's own conflicting/unverified pricing statements. The account is funded with at most $20; if GMI requires more than $20 to fund the account at all, that is a blocker to report, not grounds to fund more without further authorization. The avatar test phrase is shortened to the minimum sensible text ("Hi.") and both calls are confirmed single-shot with no retry of the paid action itself. | Pricing research found GMI's own blog and quickstart docs disagree on the avatar model's billing basis (flat per-request vs. per-second), and the voice-clone price is unverified on any official static page — a precise in-code ceiling computed from that uncertain data was less trustworthy than funding the account itself as the hard limit. The founder explicitly authorized the $20 boundary. | Founder | Supersedes the $1.20 ceiling in D-015 |
| D-018 | 2026-07-21 | APPROVED | PR #2 (CF-VERIFY-001) is merged into `main` at merge commit `f195833bd67236346f9865485be5b6a8424e3573`. `CF-RUN-001` (smallest real product flow: upload one authorized creator video -> real analysis or honest blocked state -> persist -> display) is dispatched as the next active work packet directly, in the same instruction, without waiting for a separately-formatted packet. Scope explicitly excludes voice cloning, avatar generation, publishing, billing, authentication expansion, and unrelated UI. | Directed as the next action after CF-VERIFY-001's merge, matching the same merge-and-dispatch pattern as D-012/D-013. | Controller/CTO | — |
| D-019 | 2026-07-21 | APPROVED | `CF-RUN-001`'s forbidden-paths list is amended to permit exactly one CI file, `.github/workflows/cf-run-001-tests.yml`, which runs `pytest tests/` on every pull request and push to `main`. No secrets required (the full suite is mocked/local); no deployment; no other workflow or CI change authorized. | Local test output alone could not satisfy the required CI status-check gate; a narrowly scoped, secret-free workflow provides a real terminal CI result without expanding scope beyond running the existing test suite. | Controller/CTO | Narrows the CI-file prohibition in `CF-RUN-001` for this one file only |
| D-020 | 2026-07-21 | APPROVED | PR #3 (`CF-RUN-001`) is approved as-is at exact SHA `1cb15c937422517657c3e85463df4e440a11bfb0` (76 tests passing locally and in CI) and merged into `main` at merge commit `c9ae8bb3926c2589043d8467244dc7f51586543f` — the first runtime application code merged in this repository. Real GMI analysis and real B2 persistence remain unverified live (`app.analysis.REQUEST_SHAPE_VERIFIED = False`; both credentials absent in this environment); this is not claimed as live-verified functionality. | Explicit founder confirmation was obtained for this specific merge before it was performed, distinct from the technical/CI verification that preceded it, consistent with `docs/ops/AUTHORITY_MATRIX.md`'s requirement that first release requires founder approval. | Founder | — |
| D-021 | 2026-07-21 | APPROVED | PR #5 (UI refresh: desktop `.page-shell` max-width corrected from a measured 1180px to 1280px inside a 1440px viewport, the successful-analysis result restructured into content-patterns/tone/hooks/visual-style/recommendations sections built only from real persisted `CreatorProfile` fields with honest empty-state fallbacks, accessibility fixes, and 8 new UI/route tests) is approved and merged into `main` at merge commit `e5b26eafd2917cd8bbfffa607f554195106c6a47` (84 tests passing locally and in CI). PR #4 (an earlier docs-only draft that recorded only the D-020/`CF-RUN-001` merge) is closed unmerged as superseded, since it predated and did not account for this second merge; both merges are recorded together in this update instead. | Standing founder instruction authorized continuous work across merges without a per-step approval gate; folding both merge records into one accurate, current update avoided leaving PR #4's record stale the moment it would have merged. | Founder (standing instruction) | Supersedes the unmerged PR #4 draft |
| D-022 | 2026-07-21 | SUPERSEDED by D-023 | `CF-RUN-002` is dispatched as the next active work packet: when the founder supplies real `GMI_API_KEY` and B2 credentials, wire them in (no new code is required — they are already read from environment variables) and run the first genuinely live, non-fabricated authorized creator analysis end-to-end, capturing only sanitized evidence (provider, model, confidence, whether the response validated against the contract — never the raw provider response body or any secret), and flip `app.analysis.REQUEST_SHAPE_VERIFIED` to `True` only after that real call actually succeeds. | Standing instruction directed continuing to the next bounded packet without waiting for a separate dispatch message; inspection confirmed the analysis/storage implementation is already complete, so the next real gap is credential-gated live execution, not new application code. | Founder (standing instruction) | Superseded by D-023's `CF-01`/`CF-02`, which cover the same substance under the founder's newly-issued block IDs |
| D-023 | 2026-07-21 | APPROVED | The founder's `CampaignForge Execution Ledger` (`docs/roadmap/CAMPAIGNFORGE_EXECUTION_LEDGER.md`) is received as binding doctrine and supersedes ad-hoc packet naming: `CF-RUN-002` (D-022) is superseded by the ledger's `CF-01`/`CF-02`/`CF-03`, which cover the same substance (credential readiness, live provider probe, first end-to-end analysis) under the founder's own block IDs. `CF-00` (repository/state reconciliation) is executed in this same change: local `main` re-verified equal to remote at `e5b26eafd2917cd8bbfffa607f554195106c6a47`, then `f201ba5fbca15326f81ef3605dd281c88f0d0f4b` after merging PR #6 (docs-only, CI-green, mergeable, within already-granted documentation-synchronization authority); no other open PRs or dirty working-tree changes found; 84-test baseline re-confirmed; the styled home/project pages re-verified live through a running server. `docs/roadmap/CURRENT_ROADMAP.md` is annotated (not rewritten) to note the ledger now controls near-term sequencing, since its contest-phase framing and the ledger's dad's-content-accuracy-first framing were not explicitly reconciled by the founder in the same message — flagged as an open question rather than silently resolved. | The founder issued a detailed, self-contained execution ledger with its own evidence-state vocabulary, block IDs, and proof gates; treating it as authoritative and reconciling prior ad-hoc naming against it is more truthful than maintaining two parallel "next packet" labels for the same work. | Founder | Supersedes D-022; annotates but does not rewrite `CURRENT_ROADMAP.md` |
| D-024 | 2026-07-22 | APPROVED | Routine in-ledger changes may proceed through autonomous exact-SHA R1/R2 review and automatic merge after terminal CI success, without returning to the founder for each one. R1 (technical/security/regression/consistency) and R2 (product-truthfulness/ledger-canon-alignment/scope) are independent clean-context reviews of the identical candidate SHA; a reviewer may not review changes it authored; if either returns `REVISE`, every blocker is repaired, a new SHA is created, CI reruns, and both reviews repeat since prior approvals are invalidated. Founder approval remains reserved for: spending, credential entry, public deployment, consent questions, destructive/irreversible actions, and material product-scope decisions. This does not create a "self-approval" path — R1/R2 are the approval, performed by parties independent of the authoring context, exactly as the Authority Matrix already required for merge, just without a human in the loop for routine, already-in-scope work. A separate, platform-level merge-action classifier is not part of this authority model and is not something this decision can or should override — if that classifier blocks a merge invocation, the block is recorded and reported, not routed around, and the PR is left open for a human to merge. | The founder judged that requiring a return to chat for every routine, already-authorized docs/test/CI/accessibility change was pure overhead once a real independent review substitutes for founder review — the goal is fewer unnecessary interruptions, not fewer real checks. | Founder | Extends the merge-authority language in `docs/ops/AUTHORITY_MATRIX.md`'s "Merge/release rule" |

## Superseded or corrected claims

- "Daily spending report scheduled" is not verified and must not be repeated as completed.
- "GMI key connected" and "B2 upload works" are reported historical user tests, not repository-verified current capabilities.
- A mock video is not real generation.
- No agent works after a chat turn ends unless an explicit automation exists and is verified.
- The D-005 decision to exclude face and voice cloning is corrected: authorized face-and-voice video generation is core, undeferred product scope, subject to verified per-person consent (see D-010, D-011). Any document or statement implying face/voice is optional, contest-only, or removed is stale and must be corrected on sight.

## Founder approval record

When the founder approves a proposed decision, change its status to `APPROVED`, add the approval date/context, and record any correction as a new row. Never overwrite history silently.

- 2026-07-21: Founder Braxton Vance issued a binding correction rejecting D-005 and approving D-010/D-011, delivered directly in the maker session and recorded here per the standing instruction to record founder rulings durably.
